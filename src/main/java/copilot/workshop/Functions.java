package copilot.workshop;

import java.util.List;

public class Functions {

    // Takes a String and returns its in reverse.
    public static String reverse(String input) {
        return null;
    }

    // This function filters out odd numbers from a list of integers.
    // It returns a new list containing only the even numbers.
    public static List<Integer> filterOdds(List<Integer> numbers) {
        return null;
     }

    // Schreibe eine Methode, die prüft, ob 'number' eine Primzahl ist
    public boolean isPrime(int number) {
        return false;
    }
}